<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Disabled state -->
    <item android:state_enabled="false">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="@color/moolre_disabled_grey" />
            <stroke android:width="1dp" android:color="@android:color/darker_gray" />
        </shape>
    </item>

    <!-- Pressed state -->
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="@color/moolre_button_bg_pressed" />
            <stroke android:width="1dp" android:color="@color/moolre_button_border" />
        </shape>
    </item>

    <!-- Default/Normal state -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="8dp" />
            <solid android:color="@color/moolre_primary" />
            <stroke android:width="1dp" android:color="@color/moolre_button_border" />
        </shape>
    </item>

</selector>
